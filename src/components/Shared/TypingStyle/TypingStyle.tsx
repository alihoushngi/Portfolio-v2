"use client";

import React from "react";

const TypingStyle = ({ children }: { children: React.ReactNode }) => {
  return <span className="my-0 mx-auto tracking-tighter">{children}</span>;
};

export default TypingStyle;
